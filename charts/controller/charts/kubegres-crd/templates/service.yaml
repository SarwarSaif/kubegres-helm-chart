apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: {{ .Values.global.controlPlaneName }}
  name: {{ .Values.global.serviceAccountName -}}-metrics-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: {{ .Values.global.controlPlaneName }}